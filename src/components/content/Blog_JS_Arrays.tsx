import * as React from 'react';
import { render } from 'react-dom';

export class BlogJSArrays extends React.Component<any, any> {
    render(){
        return (
            <h2>JavaScript Arrays Blog</h2>
        );
    }
}